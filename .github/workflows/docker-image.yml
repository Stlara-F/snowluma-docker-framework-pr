name: docker-publish

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  workflow_dispatch:
    inputs:
      image:
        description: 'DockerHub 镜像名 (如 yourname/snowluma)，配合 tag 构成推送目标'
        required: false
        default: ""
      tag:
        description: 'DockerHub 镜像标签 (如 test、v1.0)'
        required: true
        default: ""
      push:
        description: '构建完成后推送到 DockerHub'
        required: true
        type: boolean
        default: true
      platforms:
        description: '目标平台，多平台逗号分隔 (默认 linux/amd64,linux/arm64)'
        required: true
        default: "linux/amd64,linux/arm64"
      snowluma_tag:
        description: '【可选】从 GitHub Release 下载预编译包 (如 v1.8.5)，留空则使用仓库中已同步的 tarball'
        required: false
        default: ""
      snowluma_repository:
        description: '【配合 snowluma_tag】上游仓库 owner/repo (默认 SnowLuma/SnowLuma)'
        required: false
        default: "SnowLuma/SnowLuma"

jobs:
  resolve:
    runs-on: ubuntu-22.04
    outputs:
      matrix: ${{ steps.matrix.outputs.matrix }}
    steps:
      - name: Resolve target platforms
        id: matrix
        env:
          PLATFORMS: ${{ inputs.platforms }}
        run: |
          set -eu
          matrix='{"include":['
          separator=''
          case ",${PLATFORMS}," in
            *",linux/amd64,"*)
              matrix="${matrix}${separator}{\"arch\":\"amd64\",\"platform\":\"linux/amd64\",\"os\":\"ubuntu-22.04\"}"
              separator=','
              ;;
          esac
          case ",${PLATFORMS}," in
            *",linux/arm64,"*)
              matrix="${matrix}${separator}{\"arch\":\"arm64\",\"platform\":\"linux/arm64\",\"os\":\"ubuntu-22.04-arm\"}"
              separator=','
              ;;
          esac
          matrix="${matrix}]}"
          if [ "${matrix}" = '{"include":[]}' ]; then
            echo "No supported platforms selected. Use linux/amd64, linux/arm64, or both." >&2
            exit 1
          fi
          echo "matrix=${matrix}" >> "${GITHUB_OUTPUT}"

  build:
    needs: resolve
    strategy:
      fail-fast: false
      matrix: ${{ fromJson(needs.resolve.outputs.matrix) }}
    runs-on: ${{ matrix.os }}
    environment: SnowLuma
    steps:
      - name: Checkout Docker framework
        uses: actions/checkout@v6

      - name: Prepare SnowLuma.Framework.tar.gz
        env:
          GH_TOKEN: ${{ github.token }}
          SNOWLUMA_REPO: ${{ inputs.snowluma_repository }}
          SNOWLUMA_TAG: ${{ inputs.snowluma_tag }}
          PLATFORM: ${{ matrix.platform }}
        run: |
          chmod +x ./scripts/resolve-framework-tarball.sh
          ./scripts/resolve-framework-tarball.sh
          ls -lh SnowLuma.Framework.tar.gz

      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        if: ${{ inputs.push }}
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build image
        env:
          IMAGE: ${{ inputs.image }}
          TAG: ${{ inputs.tag }}
          PUSH: ${{ inputs.push }}
          PLATFORM: ${{ matrix.platform }}
        run: |
          set -eu
          IMAGE="${IMAGE:-${{ vars.DOCKER_IMAGE }}}"
          if [ -z "${IMAGE}" ]; then
            echo "workflow input image is required (set vars.DOCKER_IMAGE in repo settings)" >&2
            exit 1
          fi
          if [ -z "${TAG}" ]; then
            echo "workflow input tag is required"
            exit 1
          fi
          if [ "${PUSH}" = "true" ]; then
            docker buildx build \
              --platform "${PLATFORM}" \
              --file ./Dockerfile \
              --output "type=image,push-by-digest=true,name-canonical=true,push=true" \
              --metadata-file metadata.json \
              .
            digest="$(jq -r '."containerimage.digest"' metadata.json)"
            if [ -z "${digest}" ] || [ "${digest}" = "null" ]; then
              echo "Unable to resolve pushed image digest" >&2
              cat metadata.json >&2
              exit 1
            fi
            mkdir -p digests
            touch "digests/${digest#sha256:}"
          else
            docker buildx build \
              --platform "${PLATFORM}" \
              --tag "${IMAGE}:${TAG}" \
              --file ./Dockerfile \
              --load \
              .
          fi

      - name: Upload Docker image digest
        if: ${{ inputs.push }}
        uses: actions/upload-artifact@v7
        with:
          name: docker-digest-${{ matrix.arch }}
          path: digests/*
          if-no-files-found: error
          retention-days: 1

      - name: Docker logout
        if: ${{ always() && inputs.push }}
        run: docker logout || true

  merge:
    if: ${{ inputs.push }}
    needs:
      - resolve
      - build
    runs-on: ubuntu-22.04
    environment: SnowLuma
    steps:
      - name: Set up Docker buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Download Docker image digests
        uses: actions/download-artifact@v7
        with:
          pattern: docker-digest-*
          path: digests
          merge-multiple: true

      - name: Create Docker manifest
        env:
          IMAGE: ${{ inputs.image || vars.DOCKER_IMAGE }}
        run: |
          set -euo pipefail
          image="${IMAGE}"
          tag="${{ inputs.tag }}"
          cd digests
          shopt -s nullglob
          digest_files=(*)
          if [ "${#digest_files[@]}" -eq 0 ]; then
            echo "No Docker image digests were produced" >&2
            exit 1
          fi
          refs=()
          for digest in "${digest_files[@]}"; do
            refs+=("${image}@sha256:${digest}")
          done
          # 仅使用用户指定的标签，不自动添加 latest
          tags=(--tag "${image}:${tag}")
          docker buildx imagetools create "${tags[@]}" "${refs[@]}"
          docker buildx imagetools inspect "${image}:${tag}"

      - name: Docker logout
        if: always()
        run: docker logout || true
